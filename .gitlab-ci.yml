stages:
  - lint
  - test
  - report
  - build
  - deploy

default:
  image: node:22.11

eslint:
  stage: lint
  cache:
    paths:
      - node_modules/
  script:
    - npm ci
    - npm run lint:ci

type-check:
  stage: lint
  cache:
    paths:
      - node_modules/
  script:
    - npm ci
    - npm run type-check

test:
  stage: test
  cache:
    paths:
      - node_modules/
  script:
    - npm ci
    - cp $ENV_FILE .env
    - npm test
  artifacts:
    paths:
      - reports/allure-results

allure:
  stage: report
  dependencies:
    - test
  image: registry.gitlab.com/freepad/otus-qajs-2025/allure:latest
  script:
    - npx allure@2 generate allure-results --clean
  artifacts:
    paths:
      - allure-report

build:
  image: docker:20.10.16
  stage: build
  services:
    - docker:20.10.16-dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE:latest -t $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG .
    - docker push $CI_REGISTRY_IMAGE:latest
    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG

pages:
  stage: deploy
  image: alpine:latest
  dependencies:
    - allure
  script:
    - mkdir -p public
    - cp -r allure-report/* public/
  artifacts:
    paths:
      - public
